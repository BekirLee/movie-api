import chaiHttp from "chai-http";
import chai from 'chai'
import server from "../app.js";
const should = chaiDependencie.should();

chai.use(chaiHttp);

describe('Node server tests', () => {
    it("(GET/ ) for the main page! ", (done) => {
        done()
    })
})